import java.awt.*;
import javax.swing.*;
import java.util.*;

public class GUI extends JFrame{
	//pre: name is initialized and valid
	//post: GUI is setup
	public GUI(String name){
		setTitle(name);
		setSize(250,300);
		setLocation(300,20);
		
	}
}
